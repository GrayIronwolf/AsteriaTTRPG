// Retired one-time importer. Original implementation remains in Git history.
console.error('This importer predates the canonical compendium and cannot safely overwrite it. Add or edit content/<domain>/<slug>/index.md using content/README.md, then run npm run content:build.');
process.exitCode = 1;
